package com.example.campus_proj.repository;
import com.example.campus_proj.Entity.ReferenceRoom;
import org.springframework.data.jpa.repository.JpaRepository;

public interface IReferenceRoomRepository extends JpaRepository<ReferenceRoom, Long> {
}
